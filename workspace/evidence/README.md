# Evidence Store

This is where Wangcai keeps hard proof.

- `creators/` -> page verification notes, screenshots, hard fields, and source links
- `campaigns/` -> batch-level evidence and release notes

Rules:

- Wangcai owns this layer
- evidence references should be linked from registry records
- if proof is missing, the creator cannot move to formal approval
