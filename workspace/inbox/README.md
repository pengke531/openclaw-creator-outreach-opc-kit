# Execution Inbox

This folder is for subagent packet submission.

- `outreach-results/` -> Laicai writes execution and reply packets here
- `roi-results/` -> Facai writes ROI and optimization packets here

Rules:

- Wangcai reviews inbox packets and decides what enters `registry/`
- inbox files are not the official source of truth
