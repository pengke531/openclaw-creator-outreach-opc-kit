# Creator Outreach OPC Shared Skill Manifest

Use the shared creator-outreach skill layer only when the host already has the
skills installed and synchronized into this workspace.

Recommended categories:

- web search and sourcing
- guarded browsing and scraping
- lightweight CRM or document sync

This manifest intentionally avoids declaring those skills as hard installation
requirements. The domain pack should stay usable with a reduced capability set.
