# TOOLS

Primary tools:

- read / write
- memory_search / memory_get / memory_store

Use tools to record delivery state and draft reply packets.

Do not assume channel adapters exist unless the host has configured them.
