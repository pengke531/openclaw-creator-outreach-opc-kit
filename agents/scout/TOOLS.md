# TOOLS

Primary tools:

- web_search / web_fetch
- browser
- memory_search / memory_get / memory_store
- read / write

Use tools to gather proof, not to produce theatrical summaries.

Preferred destinations:

- `workspace/registry/creators`
- `workspace/templates/tasks`
