# TOOLS

Primary tools:

- read / write
- memory_search / memory_get / memory_store
- web_search / web_fetch

Use tools to compare campaign assumptions, inspect policy context, and write review packets.
